<%= form_for(compte) do |f| %>
  <% if compte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(compte.errors.count, "error") %> prohibited this compte from being saved:</h2>

      <ul>
      <% compte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <div class="field">
        <%= f.label :nom %>
        <%= f.text_field :nom %>
      </div>

      <div class="field">
        <%= f.label :titre_espace %>
        <%= f.text_field :titre_espace %>
      </div>
        <%= f.hidden_field :user_id, value: current_user.id %>
        <%= hidden_field :user, value: current_user %>
      <div class="actions">
        <%= f.submit "Valider", class: "success button" %>
      </div>

<% end %>